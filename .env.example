# Local-only development defaults. Copy to .env; never use these values outside localhost.
COMPOSE_PROJECT_NAME=atrisk
POSTGRES_DB=atrisk
POSTGRES_USER=atrisk
POSTGRES_PASSWORD=atlasrisk-local-only
POSTGRES_PORT=55432
GARAGE_ACCESS_KEY=GKatlasrisklocal
GARAGE_SECRET_KEY=atlasrisk-garage-local-only-secret
GARAGE_BUCKET=atlasrisk-raw
GARAGE_S3_PORT=53900
